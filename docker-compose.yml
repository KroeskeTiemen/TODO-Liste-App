services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    ports:
      - "5001:5001"
    environment:
      - API_BASE=http://api:5000
    depends_on:
      - api
